#include<bits/stdc++.h>

using namespace std;
typedef long long int ll;

int main() {
    int t;
    cin >> t;
    while (t--) {
                
        ll m , a,b,c;
        cin >> m >> a >> b >> c ;

        cout<<min(2*m,min(m,a)+min(m,b)+c)<<endl ;





    }

    return 0;
}